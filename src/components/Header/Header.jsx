import "./header.css";
import Navbar from "../Navbar/Navbar";

function Header(){
    return (
        <header>
            <span>LOGO</span>
            <Navbar/>
        </header>
    );
}

export default Header;
